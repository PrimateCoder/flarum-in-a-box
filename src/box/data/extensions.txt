# Extensions to enable on first boot.
# One extension ID per line, sorted alphabetically. Lines starting with #
# and blank lines are ignored. Extension IDs use dashes (e.g.
# fof-best-answer), NOT slashes — they are what `php flarum extension:enable
# <id>` expects.
#
# NOTE (Flarum 1.x quirk): unlike 2.x, a YAML install does NOT enable the
# bundled flarum/* extensions when the `extensions:` config key is absent —
# so the bundled whitelist IS listed here explicitly (approval included).
# The default-enabled set mirrors the 2.x edition on main: never enable
# here what main doesn't enable by default.
#
# To install a new extension:
#   1. Add the composer package name to src/box/composer.json.TEMPLATE
#      (also alphabetically sorted)
#   2. Add the corresponding extension ID here to enable it by default
#      (or omit here to leave it installed-but-disabled)

antoinefr-bbcode-fa
# flarum-flags must precede flarum-approval (approval requires flags).
flarum-flags
flarum-approval
flarum-bbcode
flarum-emoji
flarum-extension-manager
flarum-flags
flarum-lang-english
flarum-likes
flarum-lock
flarum-markdown
flarum-mentions
flarum-nicknames
flarum-statistics
flarum-sticky
flarum-subscriptions
flarum-suspend
flarum-tags
fof-best-answer
fof-byobu
fof-discussion-views
fof-drafts
fof-follow-tags
fof-formatting
fof-gamification
fof-ignore-users
fof-impersonate
fof-links
fof-merge-discussions
fof-moderator-notes
fof-move-posts
fof-photoswipe
fof-polls
fof-reactions
fof-sitemap
fof-split
fof-upload
fof-user-bio
fof-user-directory
ianm-log-viewer
michaelbelgium-profile-views
pianotell-demo-auto-confirm
pianotell-flamoji
rob006-last-post-avatar
